server:
  port: 8100

logging:
  level:
    pl:
      kukla:
        krzys: debug

spring:
  application:
    name: 05-currency-convertion-service
  cloud:
    config:
      uri: http://localhost:8888

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka

04-currency-exchange-service:
  ribbon:
    listOfServers: http://localhost:8000, http://localhost:8001, http://localhost:8002